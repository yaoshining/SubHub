## 临时验证日志不得写入项目目录

- 一次性的验证输出（测试日志、typecheck/format 结果等）必须写入 `/tmp/` 或 `os.tmpdir()`，不得写入项目目录（如 `.subhub/`）。
- 项目目录只保留持续有效的产物（如 SQLite 数据库文件）。
- 违反此习惯的用户批评已收到，不再重复。若需要捕获 terminal 输出用于后续分析，使用 `>/tmp/subhub-<有意义的名字>.log` 而不是项目下的隐藏目录。
